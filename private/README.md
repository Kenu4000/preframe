# private

原作データとローカル専用設定の置き場です。このREADME以外は `.gitignore` によりGit管理対象外です。

シナリオ、制御データ、画像、BGM、SE、Voiceは `private/kanon_original/` の種類別フォルダに読み取り専用で配置してください。変換結果は `cache/kanon/` へ生成します。

Voiceはキャラクター別フォルダ構造を保ったまま `private/kanon_original/voice/` に配置し、再生位置は後から手動指示として原シナリオへ非破壊で重ねます。
